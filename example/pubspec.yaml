name: flt_sdk_example
description: Demonstrates how to use the flt_sdk package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0
  flutter: ">=3.9.0"

dependencies:
  flutter:
    sdk: flutter
  flt_sdk:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true